# Security Policy

## 报告安全问题

请不要在公开 issue 中披露未修复漏洞、模型服务密钥或学生数据。应将复现步骤、受影响版本和建议修复方式私下发送给项目维护者。

## 部署基线

- 撤销历史版本中出现过的所有 API 密钥。
- 使用 HTTPS，并设置 `SESSION_COOKIE_SECURE=1`。
- 使用高强度固定 `FLASK_SECRET_KEY`；不得在多进程间随机生成。
- 将 `instance/` 和实验导出文件置于 Web 根目录之外。
- 只向已授权教师开放学生记录，定期复核教师所管理班级。
- 禁止把真实姓名、账号、对话或数据库提交到版本库。
- 在正式实验前运行自动化测试和小规模并发、断网测试。

本仓库中的修复不能替代学校的数据保护、伦理审查和事件响应流程。
